# Required: server-side only. Set this in Vercel project settings too.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# Optional: route through a custom gateway (e.g. LiteLLM) instead of api.anthropic.com
# ANTHROPIC_BASE_URL=https://litellm.example.com
